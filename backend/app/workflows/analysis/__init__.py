"""Analysis pipelines — scoring and interpretation over what research gathered.

``company_rescore`` (fundamental + sentiment scores) and ``prose_regeneration`` (sparse company
reads on meaningful shift). Shared runtime/triggers/registry live one level up in ``app.workflows``.
"""
