"""Message pipelines — the delivery shapes that surface findings to the user.

``daily_digest`` (detailed email + platform digest) and ``market_pulse`` (the short brief to
iMessage/WhatsApp). Shared runtime/triggers/registry live one level up in ``app.workflows``.
"""
