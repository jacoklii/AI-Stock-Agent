# Secrets
.env

# Actual state — Postgres volume + pg_dump snapshots (per the repo structure, all gitignored)
src/backend/data/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node (frontend) — src/api/schema.d.ts is generated but committed by design
node_modules/
src/frontend/dist/

# OS / editor
.DS_Store
.playwright-mcp/
