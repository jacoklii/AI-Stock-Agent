"""Top-level ops scripts (not part of the importable app surface).

Per the repo structure, one-off operational scripts — seeding a curated universe,
backfilling embeddings after a model change — live here, never inside ``app/``.
"""
