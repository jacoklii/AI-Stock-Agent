"""Analysis pipelines — scoring and interpretation over what research gathered.

``company_rescore`` (fundamental + sentiment scores), ``prose_regeneration`` (sparse company
reads on meaningful shift), and ``significance_recheck`` (events that aged into importance).
Shared runtime/triggers/registry live one level up in ``app.workflows``.
"""
